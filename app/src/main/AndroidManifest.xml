<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my1styear">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrinciplesOfManagementActivity"
        />
        <activity android:name=".ProfessionaCommunicationActivity"
            />
        <activity android:name=".ElectricalAndElectronicActivity"
            />
        <activity android:name=".IntroToProgrammiingActivity"
            />
        <activity android:name=".LinearAlgebraActivity"
            />
        <activity android:name=".Physicsactivity"
            />
        <activity android:name=".MainActivity"
            />
        <activity android:name=".welcomeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>